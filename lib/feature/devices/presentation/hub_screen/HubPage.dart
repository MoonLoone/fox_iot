import 'package:flutter/cupertino.dart';

class HubPage extends StatelessWidget {
  static const String navId = "hub_page";
  final String deviceId;

  HubPage(this.deviceId);

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}