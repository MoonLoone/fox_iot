
import 'package:flutter/cupertino.dart';

class CameraPage extends StatelessWidget {
  static const String navId = "camera_page";
  final String deviceId;

  CameraPage(this.deviceId);

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}