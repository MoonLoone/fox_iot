import 'package:flutter/cupertino.dart';

class MotionPage extends StatelessWidget {
  static const String navId = "motion_page";
  final String deviceId;

  MotionPage(this.deviceId);

  @override
  Widget build(BuildContext context) {
    return SizedBox();
  }
}